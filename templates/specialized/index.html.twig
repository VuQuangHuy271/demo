{% extends 'base.html.twig' %}

{% block body %}
<div class="container-fluid col-md-8 text-center">
    <h2 class="text text-success mb-2">specialized List</h2>
    <form class="d-flex" action="{{ path('specialized_search') }}" method="post">
        <input class="form-control me-2" type="search" name="name" id="" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
    </form> 

        <a class="btn btn-primary mb-3" href="{{ path('specialized_add')}}">Add specialized</a> 
    <table class="table table-hover mt-3">
        <tr>
            <th>Number</th>
            <th>Specialized Name</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
        <script language = "JavaScript">
                    var num = 1
        </script>
        {% for specialized in specialized %}      
            <tr>
                <td>
                    <script language = "JavaScript">
                    document.write(num++);
                    </script>
                </td>
                <td>
                    <a style="text-decoration:none" href="{{path ('specialized_detail',{'id':specialized.id})}}">
                        {{specialized.name}}
                    </a>
                </td>
                <td>{{specialized.description}}</td>
                <td>
                    <a class="btn btn-outline-primary" href="{{ path('specialized_edit', {'id' : specialized.id }) }}">EDIT</a>
                    <a class="btn btn-outline-warning" 
                    href="{{ path('specialized_delete', {'id' : specialized.id}) }}"
                    onclick="return confirm('Do you want to delete this specialized ?');"
                    >DELETE</a>
                </td>
            </tr>
        {% endfor %}
    </table>
</div>
{% endblock %}
